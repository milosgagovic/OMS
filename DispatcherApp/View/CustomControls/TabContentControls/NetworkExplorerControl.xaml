<UserControl x:Class="DispatcherApp.View.NetworkExplorerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DispatcherApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TreeView Margin="0, 10, 0, 0" BorderThickness="0">
            <TreeViewItem Header="Sources" IsExpanded="True" ItemsSource="{Binding NetworkMapsBySource}">

            </TreeViewItem>
        </TreeView>
        <Grid Grid.Row="1" Background="White">
            <Expander Header="Sources" IsExpanded="True">
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding NetworkMapsBySourceButton}">
                    </ItemsControl>
                </StackPanel>
            </Expander>
        </Grid>
    </Grid>
</UserControl>
